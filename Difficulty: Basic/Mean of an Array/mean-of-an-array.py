# User function Template for python3

class Solution:
    def findMean(self, arr):
        # Calculate sum of all elements
        total = sum(arr)
        
        # Calculate mean
        mean = total / len(arr)
        

        return int(mean)
